<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.acer.reddoorzz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Login" />
        <activity android:name=".Buatakun" />
        <activity android:name=".Home"
            android:label="Pilih Tanggal"/>
        <activity android:name=".buat_biodata" />
        <activity android:name=".lihat_biodata" />
        <activity android:name=".update_biodata" />
        <activity android:name=".Ulasan"
            android:label="Ulasan"/>
        <activity android:name=".Detail"
        android:label="Detail Tempat"/>
        <activity android:name=".Fasilitas"
            android:label="Fasilitas"/>
        <activity android:name=".Lokasi"
            android:label="Lokasi"/>
        <activity android:name=".Foto"
            android:label="Foto"/>
        <activity android:name=".Finish"></activity>
    </application>

</manifest>